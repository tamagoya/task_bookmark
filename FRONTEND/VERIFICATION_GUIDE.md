# タスクブックマーク Chrome拡張機能 - 動作確認ガイド

## 概要

本ガイドでは、Bolt 1とBolt 2で実装された機能の動作確認手順を説明します。

**実装済み機能**:
- ✅ Googleアカウント認証（OAuth2）
- ✅ カレンダー初期化（専用カレンダーの自動作成）
- ✅ トークン自動リフレッシュ
- ✅ 認証状態の管理
- ✅ タブ情報の取得（Bolt 3）
- ✅ カレンダーイベントCRUD操作（Bolt 2）

**未実装機能**（今後のBoltで実装予定）:
- ⏳ タブ一覧のUI表示（Bolt 5）
- ⏳ 仕事状態の保存UI（Bolt 5）
- ⏳ 保存済み仕事一覧表示（Bolt 5）
- ⏳ 仕事状態の復元（Bolt 6）

---

## 前提条件

### 1. OAuth2設定の確認

`manifest.json`にOAuth2クライアントIDが設定されていることを確認してください：

```json
"oauth2": {
  "client_id": "742273768009-d2551p7a54hv181bj5quunq6usj58hlp.apps.googleusercontent.com",
  "scopes": [
    "https://www.googleapis.com/auth/calendar"
  ]
}
```

**注意**: 開発環境で異なる拡張機能IDを使用している場合、OAuth2クライアントIDを再設定する必要があります。詳細は`OAUTH2_SETUP.md`を参照してください。

### 2. Google Calendar APIの有効化

Google Cloud ConsoleでGoogle Calendar APIが有効になっていることを確認してください。

---

## ステップ1: ビルド

### 1.1 依存関係のインストール

```bash
cd FRONTEND
npm install
```

### 1.2 ビルド実行

```bash
npm run build
```

ビルドが成功すると、`FRONTEND/dist`ディレクトリに以下のファイルが生成されます：

```
dist/
├── manifest.json
├── background/
│   └── service-worker.js
└── sidepanel/
    ├── sidepanel.html
    ├── sidepanel.css
    └── sidepanel.js
```

### 1.3 ビルドエラーの確認

ビルドエラーが発生した場合：

```bash
# TypeScriptの型チェック
npm run type-check

# エラーの詳細を確認
```

---

## ステップ2: Chrome拡張機能のインストール

### 2.1 開発者モードの有効化

1. Chromeブラウザで `chrome://extensions/` を開く
2. 右上の「開発者モード」トグルをONにする

### 2.2 拡張機能の読み込み

1. 「パッケージ化されていない拡張機能を読み込む」をクリック
2. `FRONTEND/dist`ディレクトリを選択
3. 拡張機能が一覧に表示されることを確認

### 2.3 拡張機能の確認

- **名前**: タスクブックマーク
- **ID**: 拡張機能IDが表示される（例: `abcdefghijklmnopqrstuvwxyz123456`）
- **バージョン**: 0.1.0
- **エラー**: エラーが表示されていないことを確認

---

## ステップ3: 基本動作確認

### 3.1 サイドパネルの表示

1. 拡張機能のアイコンをクリック（ツールバーに表示されているはず）
2. サイドパネルが開くことを確認
3. 以下のUI要素が表示されることを確認：
   - タイトル: 「タスクブックマーク」
   - 認証状態: 「認証状態を確認中...」
   - 認証ボタン: 初期状態では非表示

### 3.2 コンソールログの確認

1. サイドパネルを右クリック → 「検証」を選択
2. 「Console」タブを開く
3. エラーメッセージがないことを確認

---

## ステップ4: 認証機能の確認

### 4.1 認証状態の確認

**期待される動作**:
- サイドパネルを開いた時点で、認証状態が自動的に確認される
- 未認証の場合、「認証する」ボタンが表示される
- 認証済みの場合、「ログアウト」ボタンが表示される

**確認手順**:
1. サイドパネルを開く
2. 認証状態のメッセージを確認
3. 表示されるボタンを確認

### 4.2 認証の実行

**期待される動作**:
1. 「認証する」ボタンをクリック
2. Googleアカウントの認証画面が表示される
3. 認証が完了すると、サイドパネルに認証済み状態が表示される
4. 専用カレンダー（「タスクブックマーク」）が自動作成される

**確認手順**:
1. 「認証する」ボタンをクリック
2. Googleアカウントの認証画面が表示されることを確認
3. 認証を完了（必要に応じてアカウントを選択、権限を許可）
4. サイドパネルに戻り、認証状態が更新されることを確認
5. Googleカレンダー（https://calendar.google.com/）を開き、「タスクブックマーク」という名前のカレンダーが作成されていることを確認

**確認ポイント**:
- ✅ 認証画面が正常に表示される
- ✅ 認証が成功する
- ✅ サイドパネルに認証済み状態が表示される
- ✅ 専用カレンダーが作成される

### 4.3 認証エラーの確認

**想定されるエラー**:

1. **"bad client id"エラー**
   - **原因**: OAuth2クライアントIDが正しく設定されていない、または拡張機能IDと一致していない
   - **対処**: `OAUTH2_SETUP.md`を参照してOAuth2クライアントIDを再設定

2. **"Access blocked"エラー**
   - **原因**: OAuth同意画面でスコープが追加されていない、またはテストユーザーが追加されていない
   - **対処**: Google Cloud ConsoleでOAuth同意画面を確認

3. **"Google Calendar API not enabled"エラー**
   - **原因**: Google Calendar APIが有効になっていない
   - **対処**: Google Cloud ConsoleでGoogle Calendar APIを有効化

### 4.4 ログアウトの確認

**期待される動作**:
1. 「ログアウト」ボタンをクリック
2. 認証状態がクリアされる
3. 「認証する」ボタンが再表示される

**確認手順**:
1. 認証済み状態で「ログアウト」ボタンをクリック
2. 認証状態が「未認証」に戻ることを確認
3. 「認証する」ボタンが表示されることを確認

---

## ステップ5: カレンダー初期化の確認

### 5.1 専用カレンダーの作成確認

**期待される動作**:
- 初回認証時に、「タスクブックマーク」という名前の専用カレンダーが自動作成される
- 既に同名のカレンダーが存在する場合、既存のカレンダーを使用する

**確認手順**:
1. Googleカレンダー（https://calendar.google.com/）を開く
2. 左側のカレンダー一覧で「タスクブックマーク」という名前のカレンダーを探す
3. カレンダーが存在することを確認
4. カレンダーの色や表示設定を確認（任意）

### 5.2 既存カレンダーの再利用確認

**確認手順**:
1. 一度ログアウトする
2. 再度認証する
3. 新しいカレンダーが作成されず、既存のカレンダーが使用されることを確認

---

## ステップ6: トークンリフレッシュの確認

### 6.1 自動リフレッシュの確認

**期待される動作**:
- 30分ごとにトークンが自動的にリフレッシュされる
- ユーザーは何も操作する必要がない

**確認手順**:
1. 認証を完了する
2. 30分待つ（または、開発者ツールでアラームを手動でトリガー）
3. コンソールログでトークンリフレッシュのログを確認

**注意**: 実際に30分待つのは現実的ではないため、開発者ツールでアラームを手動でトリガーする方法を推奨します。

### 6.2 アラームの手動トリガー（開発用）

1. `chrome://extensions/` を開く
2. 拡張機能の「詳細」をクリック
3. 「サービスワーカー」のリンクをクリック
4. 開発者ツールの「Console」タブで以下を実行：

```javascript
// アラームを手動でトリガー
chrome.alarms.create('refreshToken', { delayInMinutes: 0 });
```

5. コンソールログでトークンリフレッシュのログを確認

---

## ステップ7: エラーハンドリングの確認

### 7.1 ネットワークエラーの確認

**確認手順**:
1. インターネット接続を一時的に切断
2. 認証を試みる
3. 適切なエラーメッセージが表示されることを確認
4. インターネット接続を復元
5. 再度認証を試み、正常に動作することを確認

### 7.2 権限エラーの確認

**確認手順**:
1. Google Cloud ConsoleでGoogle Calendar APIを一時的に無効化
2. 認証を試みる
3. 適切なエラーメッセージが表示されることを確認
4. Google Calendar APIを再有効化
5. 再度認証を試み、正常に動作することを確認

---

## ステップ8: ストレージの確認

### 8.1 Chrome Storageへの保存確認

**確認手順**:
1. 認証を完了する
2. `chrome://extensions/` を開く
3. 拡張機能の「詳細」をクリック
4. 「ストレージ」セクションを確認
5. 以下のデータが保存されていることを確認：
   - `authState`: 認証状態（アクセストークン、リフレッシュトークン、カレンダーIDなど）

### 8.2 ストレージのクリア確認

**確認手順**:
1. 認証を完了する
2. 「ストレージ」セクションで「クリア」をクリック
3. サイドパネルを再読み込み
4. 認証状態が「未認証」に戻ることを確認

---

## ステップ9: 開発者ツールでの確認

### 9.1 Service Workerの確認

1. `chrome://extensions/` を開く
2. 拡張機能の「詳細」をクリック
3. 「サービスワーカー」のリンクをクリック
4. 開発者ツールが開き、Service Workerのログを確認できる

### 9.2 コンソールログの確認

**確認すべきログ**:
- 拡張機能のインストール時のログ
- 認証処理のログ
- カレンダー初期化のログ
- トークンリフレッシュのログ
- エラーログ（発生した場合）

### 9.3 ネットワークリクエストの確認

1. Service Workerの開発者ツールで「Network」タブを開く
2. 認証を実行
3. 以下のAPIリクエストが送信されることを確認：
   - `https://www.googleapis.com/oauth2/v2/userinfo`（認証情報取得）
   - `https://www.googleapis.com/calendar/v3/calendars`（カレンダー一覧取得）
   - `https://www.googleapis.com/calendar/v3/calendars`（カレンダー作成、必要に応じて）

---

## トラブルシューティング

### 問題1: サイドパネルが開かない

**原因**:
- 拡張機能が正しくインストールされていない
- Service Workerにエラーがある

**対処**:
1. `chrome://extensions/` で拡張機能のエラーを確認
2. Service Workerのログを確認
3. 拡張機能を再読み込み

### 問題2: 認証ボタンが表示されない

**原因**:
- JavaScriptエラーが発生している
- メッセージハンドラーが正しく動作していない

**対処**:
1. サイドパネルの開発者ツールでコンソールエラーを確認
2. Service Workerのログを確認
3. `sidepanel.ts`のコードを確認

### 問題3: 認証が失敗する

**原因**:
- OAuth2クライアントIDが正しく設定されていない
- Google Calendar APIが有効になっていない
- テストユーザーが追加されていない（テストモードの場合）

**対処**:
1. `OAUTH2_SETUP.md`を参照してOAuth2設定を確認
2. Google Cloud ConsoleでAPIの有効化を確認
3. OAuth同意画面でテストユーザーを確認

### 問題4: カレンダーが作成されない

**原因**:
- 認証が完了していない
- Google Calendar APIの権限がない
- APIリクエストが失敗している

**対処**:
1. 認証状態を確認
2. Service Workerのログでエラーを確認
3. ネットワークリクエストのレスポンスを確認

---

## 確認チェックリスト

### 基本動作
- [ ] 拡張機能が正常にインストールできる
- [ ] サイドパネルが正常に開く
- [ ] UI要素が正しく表示される
- [ ] エラーログが表示されない

### 認証機能
- [ ] 認証状態が正しく確認される
- [ ] 「認証する」ボタンが表示される（未認証時）
- [ ] 認証が正常に完了する
- [ ] 認証済み状態が正しく表示される
- [ ] 「ログアウト」ボタンが表示される（認証済み時）
- [ ] ログアウトが正常に動作する

### カレンダー機能
- [ ] 専用カレンダーが自動作成される
- [ ] 既存カレンダーが正しく再利用される

### ストレージ
- [ ] 認証情報がChrome Storageに保存される
- [ ] ストレージのクリアが正常に動作する

### エラーハンドリング
- [ ] ネットワークエラーが適切に処理される
- [ ] 権限エラーが適切に処理される
- [ ] エラーメッセージが適切に表示される

---

## ステップ10: タブキャプチャ機能の確認（Bolt 3）

### 10.1 タブキャプチャの概要

Bolt 3では、現在のウィンドウのタブ情報を取得する機能が実装されました。この機能はまだUIには統合されていないため、Service Workerのコンソールから手動でテストします。

### 10.2 タブキャプチャのテスト

**準備**:
1. いくつかのタブを開いておく（3〜5個程度）
2. 各タブは異なるWebサイトを開いていることを確認

**テスト手順**:

1. `chrome://extensions/` を開く
2. 拡張機能の「詳細」をクリック
3. 「サービスワーカー」のリンクをクリック（開発者ツールが開く）
4. 「Console」タブで以下のコードを実行：

```javascript
// Service Workerから全タブを取得
(async () => {
  // すべてのウィンドウからタブを取得
  // 注意: Service Workerでは currentWindow: true は機能しません
  const tabs = await chrome.tabs.query({});
  
  console.log('=== タブキャプチャ結果 ===');
  console.log(`取得したタブ数: ${tabs.length}`);
  
  // ウィンドウごとにグループ化
  const tabsByWindow = {};
  tabs.forEach(tab => {
    if (!tabsByWindow[tab.windowId]) {
      tabsByWindow[tab.windowId] = [];
    }
    tabsByWindow[tab.windowId].push(tab);
  });
  
  // ウィンドウごとに表示
  Object.entries(tabsByWindow).forEach(([windowId, windowTabs]) => {
    console.log(`\n=== Window ${windowId} (${windowTabs.length} tabs) ===`);
    windowTabs.forEach((tab, index) => {
      console.log(`[Tab ${index + 1}]`);
      console.log(`  URL: ${tab.url}`);
      console.log(`  タイトル: ${tab.title}`);
      console.log(`  ファビコン: ${tab.favIconUrl || '(なし)'}`);
      console.log(`  インデックス: ${tab.index}`);
    });
  });
  
  return tabs;
})();
```

**注意**: Service Workerは特定のウィンドウに紐付いていないため、`currentWindow: true` を使用すると空の配列が返されます。実際のアプリケーションでは、サイドパネルやポップアップから呼び出すか、`chrome.windows.getCurrent()` でウィンドウIDを取得してから指定します。

**期待される結果**:
- ✅ すべてのウィンドウのタブ情報が取得できる
- ✅ 各タブのURL、タイトル、ファビコンURL、インデックスが正しく表示される
- ✅ タブの順序（インデックス）が保持されている
- ✅ ウィンドウごとにタブがグループ化されて表示される

### 10.3 タブキャプチャの詳細テスト

**パフォーマンステスト**:

```javascript
// パフォーマンステスト：タブ取得時間を測定
(async () => {
  const startTime = performance.now();
  // Service Workerでは currentWindow が機能しないため、全タブを取得
  const tabs = await chrome.tabs.query({});
  const endTime = performance.now();
  
  console.log(`=== パフォーマンス結果 ===`);
  console.log(`全タブ数: ${tabs.length}`);
  console.log(`取得時間: ${(endTime - startTime).toFixed(2)}ms`);
  
  // 目標: 20タブで500ms以内
  if (endTime - startTime <= 500) {
    console.log('✅ パフォーマンス要件を満たしています');
  } else {
    console.log('⚠️ パフォーマンス要件を確認してください');
  }
})();
```

**エラーハンドリングテスト**:

```javascript
// エラーハンドリングテスト
(async () => {
  try {
    // 存在しないタブIDでテスト
    await chrome.tabs.get(999999);
    console.log('❌ エラーがスローされるべきでした');
  } catch (error) {
    console.log('✅ エラーが正しくキャッチされました:', error.message);
  }
})();
```

### 10.4 タブキャプチャの確認チェックリスト

- [x] すべてのタブ情報が取得できる
- [x] タブのURL、タイトル、ファビコンURLが正しく取得できる
- [x] タブのインデックス（順序）が保持されている
- [x] パフォーマンス要件を満たしている
- [x] エラーが適切にハンドリングされる

**確認完了日**: 2026-01-22

---

## ステップ11: 仕事状態の保存機能の確認（Bolt 4）

### 11.1 保存機能の概要

Bolt 4では、現在のタブ状態をGoogle Calendarにイベントとして保存する機能が実装されました。

**実装済み機能**:
- ✅ 現在のタブ一覧の表示
- ✅ 保存フォーム（仕事名、メモ入力）
- ✅ カレンダーイベントへの保存
- ✅ 成功/失敗メッセージの表示

### 11.2 前提条件

- ✅ Bolt 1-3の動作確認が完了している
- ✅ Googleアカウントで認証済み
- ✅ 専用カレンダーが作成されている
- ✅ いくつかのタブが開いている（3〜5個程度）

### 11.3 タブ一覧表示の確認

**期待される動作**:
- サイドパネルを開くと、現在のウィンドウのタブ一覧が表示される
- 各タブについて、ファビコン、タイトル、URLが表示される
- タブの順序が保持される

**確認手順**:
1. いくつかのタブを開く（異なるWebサイト）
2. 拡張機能のアイコンをクリックしてサイドパネルを開く
3. 「現在のタブ」セクションが表示されることを確認
4. タブ数が正しく表示されることを確認
5. 各タブのファビコン、タイトル、URLが正しく表示されることを確認

**確認ポイント**:
- ✅ タブ一覧が表示される
- ✅ タブ数が正しい
- ✅ ファビコンが表示される（取得できない場合はデフォルトアイコン）
- ✅ タイトルとURLが正しく表示される
- ✅ タブの順序が保持されている

### 11.4 保存フォームの確認

**期待される動作**:
- 認証済みの場合、保存フォームが表示される
- 仕事名（必須）とメモ（任意）を入力できる
- 保存ボタンが表示される

**確認手順**:
1. サイドパネルを開く
2. 「仕事状態を保存」セクションが表示されることを確認
3. 仕事名入力欄が表示されることを確認
4. メモ入力欄が表示されることを確認
5. 保存ボタンが表示されることを確認

**確認ポイント**:
- ✅ 保存フォームが表示される
- ✅ 仕事名入力欄が必須マーク（*）付きで表示される
- ✅ メモ入力欄が任意として表示される
- ✅ 保存ボタンが表示される

### 11.5 仕事状態の保存の確認

**期待される動作**:
1. 仕事名を入力して保存ボタンをクリック
2. ローディング状態が表示される（「保存中...」）
3. 保存成功時に成功メッセージが表示される
4. Googleカレンダーにイベントが作成される
5. イベントの開始時間が30分前、終了時間が現在時刻になる

**確認手順**:

#### 11.5.1 基本的な保存
1. サイドパネルを開く
2. 仕事名を入力（例: 「プロジェクトAの調査」）
3. メモを入力（任意、例: 「API仕様を確認」）
4. 「保存する」ボタンをクリック
5. ボタンが「保存中...」に変わることを確認
6. 成功メッセージ「保存しました」が表示されることを確認（3秒後に自動非表示）
7. フォームがリセットされることを確認

#### 11.5.2 Googleカレンダーでの確認
1. Googleカレンダー（https://calendar.google.com/）を開く
2. 「タスクブックマーク」カレンダーを選択
3. 保存した時刻にイベントが作成されていることを確認
4. イベントのタイトルが入力した仕事名になっていることを確認
5. イベントの開始時間が30分前、終了時間が現在時刻になっていることを確認
6. イベントの説明欄を開き、JSON形式でタブ情報が保存されていることを確認

**確認ポイント**:
- ✅ 保存ボタンをクリックするとローディング状態が表示される
- ✅ 保存成功時に成功メッセージが表示される
- ✅ フォームがリセットされる
- ✅ Googleカレンダーにイベントが作成される
- ✅ イベントのタイトルが正しい
- ✅ イベントの時間が正しい（開始: 30分前、終了: 現在時刻）
- ✅ イベントの説明欄にJSON形式でデータが保存されている

### 11.6 バリデーションの確認

**期待される動作**:
- 仕事名が空の場合、エラーメッセージが表示される
- 保存が実行されない

**確認手順**:
1. サイドパネルを開く
2. 仕事名を入力せずに「保存する」ボタンをクリック
3. エラーメッセージ「仕事名を入力してください」が表示されることを確認
4. 保存が実行されないことを確認（Googleカレンダーにイベントが作成されない）

**確認ポイント**:
- ✅ 仕事名が空の場合、エラーメッセージが表示される
- ✅ 保存が実行されない

### 11.7 エラーハンドリングの確認

**期待される動作**:
- 認証されていない場合、エラーメッセージが表示される
- タブが存在しない場合、エラーメッセージが表示される
- APIエラー時、適切なエラーメッセージが表示される

**確認手順**:

#### 11.7.1 未認証時のエラー
1. ログアウトする
2. サイドパネルを開く
3. 保存フォームが表示されないことを確認（または、保存を試みてエラーメッセージが表示されることを確認）

**確認ポイント**:
- ✅ 未認証時に適切なエラーメッセージが表示される
- ✅ エラーメッセージがユーザーフレンドリーである

**注意**: Chromeでは最後のタブを閉じることができないため、「タブがない場合」のエラーは実際には発生しません。ただし、Service Workerのコードでは `tabs.length === 0` のチェックが実装されており、万が一の場合に備えています。

### 11.8 保存時間の確認（パフォーマンス）

**期待される動作**:
- 保存時間が2秒以内（p50）で完了する

**確認手順**:
1. サイドパネルを開く
2. 開発者ツールを開く（サイドパネルを右クリック → 「検証」）
3. 「Network」タブを開く
4. 仕事名とメモを入力して「保存する」ボタンをクリック
5. ネットワークリクエストの時間を確認
6. 保存完了までの時間を確認

**確認ポイント**:
- ✅ 保存時間が2秒以内で完了する（通常のネットワーク環境）

### 11.9 保存済みイベントの内容確認

**期待される動作**:
- イベントの説明欄にJSON形式で以下の情報が保存される：
  - タブ情報の配列（URL、タイトル、ファビコンURL、インデックス）
  - 保存日時（ISO 8601形式）
  - メモ（入力した場合）
  - スキーマバージョン

**確認手順**:
1. Googleカレンダーで保存したイベントを開く
2. イベントの説明欄を確認
3. JSON形式でデータが保存されていることを確認
4. 以下のフィールドが含まれていることを確認：
   - `version`: スキーマバージョン（例: "1.0.0"）
   - `tabs`: タブ情報の配列
   - `savedAt`: 保存日時（ISO 8601形式）
   - `memo`: メモ（入力した場合）

**確認ポイント**:
- ✅ JSON形式でデータが保存されている
- ✅ すべてのタブ情報が含まれている
- ✅ 保存日時が正しい
- ✅ メモが正しく保存されている（入力した場合）
- ✅ スキーマバージョンが含まれている

### 11.10 保存機能の確認チェックリスト

- [x] タブ一覧が正しく表示される
- [x] 保存フォームが表示される
- [x] 仕事名とメモを入力できる
- [x] 保存ボタンをクリックすると保存が実行される
- [x] 保存成功時に成功メッセージが表示される
- [x] Googleカレンダーにイベントが作成される
- [x] イベントのタイトルが正しい
- [x] イベントの時間が正しい（開始: 30分前、終了: 現在時刻）
- [x] イベントの説明欄にJSON形式でデータが保存されている
- [x] バリデーションが正しく動作する
- [x] エラーハンドリングが適切に動作する
- [x] 保存時間が2秒以内で完了する

**確認完了日**: 2026-01-22

---

## ステップ12: 保存済み仕事一覧表示機能の確認（Bolt 5）

### 12.1 一覧表示機能の概要

Bolt 5では、Google Calendarから保存済みの仕事状態を取得し、サイドパネルに時系列で一覧表示する機能が実装されました。

**実装済み機能**:
- ✅ 保存済み仕事状態の一覧表示（時系列、新しい順）
- ✅ 検索機能（仕事名）
- ✅ フィルタリング機能（今日、今週、今月、すべて）
- ✅ ファビコンサムネイル表示
- ✅ ローディングインジケーター
- ✅ エラーメッセージと再試行ボタン

### 12.2 前提条件

- ✅ Bolt 1-4の動作確認が完了している
- ✅ Googleアカウントで認証済み
- ✅ 専用カレンダーが作成されている
- ✅ いくつかの仕事状態が保存されている（Bolt 4で保存したもの）

### 12.3 一覧表示の確認

**期待される動作**:
- サイドパネルを開くと、「保存済み仕事」セクションが表示される
- 保存済みの仕事状態が時系列（新しい順）で表示される
- 各仕事について、仕事名、保存日時、タブ数、ファビコンサムネイルが表示される

**確認手順**:
1. サイドパネルを開く
2. 「保存済み仕事」セクションが表示されることを確認
3. 保存済みの仕事状態が一覧表示されることを確認
4. 各仕事項目について以下を確認：
   - 仕事名が表示される
   - 保存日時が表示される（例: "今日 10:30 - 11:00"）
   - タブ数が表示される（例: "5タブ"）
   - ファビコンサムネイルが表示される（最大5個）

**確認ポイント**:
- ✅ 一覧が表示される
- ✅ 時系列（新しい順）で並んでいる
- ✅ 仕事名、保存日時、タブ数が正しく表示される
- ✅ ファビコンサムネイルが表示される（取得できない場合は非表示）

### 12.4 検索機能の確認

**期待される動作**:
- 検索入力欄に仕事名を入力すると、リアルタイムでフィルタリングされる
- 検索クエリに一致する仕事のみが表示される

**確認手順**:
1. サイドパネルを開く
2. 検索入力欄に仕事名の一部を入力（例: "プロジェクト"）
3. 検索クエリに一致する仕事のみが表示されることを確認
4. 検索クエリを削除すると、すべての仕事が再表示されることを確認

**確認ポイント**:
- ✅ 検索入力欄が表示される
- ✅ リアルタイムでフィルタリングされる
- ✅ 検索クエリに一致する仕事のみが表示される
- ✅ 検索クエリを削除すると、すべての仕事が再表示される

### 12.5 フィルタリング機能の確認

**期待される動作**:
- フィルタリングボタン（今日、今週、今月、すべて）をクリックすると、該当する日付範囲の仕事のみが表示される
- アクティブなフィルターボタンがハイライト表示される

**確認手順**:

#### 12.5.1 「今日」フィルター
1. 「今日」ボタンをクリック
2. 今日保存した仕事のみが表示されることを確認
3. 「今日」ボタンがハイライト表示されることを確認

#### 12.5.2 「今週」フィルター
1. 「今週」ボタンをクリック
2. 今週保存した仕事のみが表示されることを確認
3. 「今週」ボタンがハイライト表示されることを確認

#### 12.5.3 「今月」フィルター
1. 「今月」ボタンをクリック
2. 今月保存した仕事のみが表示されることを確認
3. 「今月」ボタンがハイライト表示されることを確認

#### 12.5.4 「すべて」フィルター
1. 「すべて」ボタンをクリック
2. 過去30日分の仕事が表示されることを確認（デフォルト）
3. 「すべて」ボタンがハイライト表示されることを確認

**確認ポイント**:
- ✅ フィルタリングボタンが表示される
- ✅ 各フィルターが正しく動作する
- ✅ アクティブなフィルターボタンがハイライト表示される
- ✅ フィルターを変更すると、一覧が更新される

### 12.6 ローディングインジケーターの確認

**期待される動作**:
- データ取得中はローディングインジケーターが表示される
- データ取得完了後、ローディングインジケーターが非表示になる

**確認手順**:
1. サイドパネルを開く
2. フィルターボタンをクリック（例: 「今週」）
3. ローディングインジケーターが表示されることを確認（一瞬の場合もある）
4. データ取得完了後、ローディングインジケーターが非表示になり、一覧が表示されることを確認

**確認ポイント**:
- ✅ ローディングインジケーターが表示される
- ✅ データ取得完了後、ローディングインジケーターが非表示になる

### 12.7 エラーハンドリングの確認

**期待される動作**:
- データ取得エラー時、エラーメッセージと再試行ボタンが表示される
- 再試行ボタンをクリックすると、再度データ取得を試みる

**確認手順**:

#### 12.7.1 エラーメッセージの表示
1. ネットワーク接続を一時的に切断（または、認証を解除）
2. サイドパネルを開く
3. エラーメッセージが表示されることを確認
4. 再試行ボタンが表示されることを確認

#### 12.7.2 再試行機能
1. ネットワーク接続を復元（または、再認証）
2. 「再試行」ボタンをクリック
3. データ取得が再実行されることを確認
4. 成功時に一覧が表示されることを確認

**確認ポイント**:
- ✅ エラーメッセージが表示される
- ✅ 再試行ボタンが表示される
- ✅ 再試行ボタンをクリックすると、再度データ取得を試みる

### 12.8 破損データの表示確認

**期待される動作**:
- データが破損している場合でも、読み込める部分（タイトル、日時）は表示される
- 破損データには「破損」バッジが表示される

**確認手順**:
1. Googleカレンダーでイベントの説明欄を手動で編集して、JSON形式を壊す
2. サイドパネルを開く
3. 破損した仕事状態が表示されることを確認
4. 「破損」バッジが表示されることを確認
5. タイトルと日時は表示されるが、タブ情報は表示されないことを確認

**確認ポイント**:
- ✅ 破損データでもタイトルと日時が表示される
- ✅ 「破損」バッジが表示される
- ✅ タブ情報が表示されない（または0タブと表示される）

### 12.9 パフォーマンスの確認

**期待される動作**:
- 過去30日分の取得が3秒以内で完了する

**確認手順**:
1. サイドパネルを開く
2. 開発者ツールを開く（サイドパネルを右クリック → 「検証」）
3. 「Network」タブを開く
4. 「すべて」フィルターを選択
5. ネットワークリクエストの時間を確認
6. データ取得完了までの時間を確認

**確認ポイント**:
- ✅ 過去30日分の取得が3秒以内で完了する（通常のネットワーク環境）

### 12.10 日付表示の確認

**期待される動作**:
- 日付が適切にフォーマットされて表示される
- 今日の場合は「今日」、昨日の場合は「昨日」と表示される

**確認手順**:
1. サイドパネルを開く
2. 各仕事項目の日時表示を確認
3. 今日保存した仕事は「今日 HH:MM - HH:MM」と表示されることを確認
4. 昨日保存した仕事は「昨日 HH:MM - HH:MM」と表示されることを確認
5. それ以前の仕事は「M/D HH:MM - HH:MM」と表示されることを確認

**確認ポイント**:
- ✅ 日付が適切にフォーマットされて表示される
- ✅ 今日・昨日の表示が正しい
- ✅ 開始時間と終了時間が正しく表示される

### 12.11 メモ表示の確認

**期待される動作**:
- メモが入力されている場合、メモが表示される

**確認手順**:
1. メモ付きで仕事状態を保存（Bolt 4で確認済み）
2. サイドパネルを開く
3. 保存済み仕事一覧で、メモが表示されることを確認

**確認ポイント**:
- ✅ メモが入力されている場合、メモが表示される
- ✅ メモが入力されていない場合、メモは表示されない

### 12.12 保存後の自動更新確認

**期待される動作**:
- 新しい仕事状態を保存すると、一覧が自動的に更新される

**確認手順**:
1. サイドパネルを開く
2. 現在の一覧を確認
3. 新しい仕事状態を保存（Bolt 4の機能を使用）
4. 一覧が自動的に更新され、新しい仕事状態が表示されることを確認

**確認ポイント**:
- ✅ 新しい仕事状態を保存すると、一覧が自動的に更新される
- ✅ 新しい仕事状態が一覧の先頭に表示される

### 12.13 保存済み仕事一覧の確認チェックリスト

- [ ] 保存済み仕事一覧が時系列で表示される
- [ ] 仕事名、保存日時、タブ数、ファビコンサムネイルが正しく表示される
- [ ] 検索機能が正しく動作する
- [ ] フィルタリング機能が正しく動作する（今日、今週、今月、すべて）
- [ ] ローディングインジケーターが表示される
- [ ] エラーメッセージと再試行ボタンが表示される
- [ ] 破損データが適切に表示される
- [ ] 過去30日分の取得が3秒以内で完了する
- [ ] 日付表示が適切にフォーマットされている
- [ ] メモが正しく表示される
- [ ] 保存後の自動更新が動作する

**確認完了日**: 2026-01-22

---

## ステップ13: 仕事状態の復元機能の確認（Bolt 6）

### 13.1 事前準備

1. 拡張機能を再ビルドする
   ```bash
   cd FRONTEND
   npm run build
   ```

2. `chrome://extensions/` で拡張機能を再読み込み（更新ボタンをクリック）

3. いくつかの仕事状態を保存しておく（Bolt 4の機能を使用）
   - 複数のタブ（3〜5個）を開いた状態で保存
   - 仕事名とメモを入力して保存

### 13.2 復元ボタンの確認

**期待される動作**:
- 保存済み仕事一覧の各項目に「復元」ボタンが表示される

**確認手順**:
1. サイドパネルを開く
2. 認証済みであることを確認
3. 保存済み仕事一覧を確認
4. 各仕事項目に「復元」ボタンがあることを確認

**確認ポイント**:
- [ ] 各仕事項目に青い「復元」ボタンが表示されている
- [ ] ボタンはクリック可能な状態である

### 13.3 復元機能の動作確認

**期待される動作**:
- 「復元」ボタンをクリックすると、新しいウィンドウが開き、保存されたタブがすべて復元される

**確認手順**:
1. 保存済み仕事状態の「復元」ボタンをクリック
2. 「復元中...」のメッセージが表示されることを確認
3. 新しいウィンドウが開くことを確認
4. 保存されたすべてのタブが新しいウィンドウに復元されることを確認
5. 「仕事状態を復元しました（Xタブ）」のメッセージが表示されることを確認

**確認ポイント**:
- [ ] 「復元」ボタンをクリックすると「復元中...」メッセージが表示される
- [ ] 新しいウィンドウが開く
- [ ] 保存されたすべてのタブが復元される
- [ ] タブの順序が保持される
- [ ] 成功メッセージが表示される

### 13.4 タブの順序確認

**期待される動作**:
- 復元されたタブの順序が、保存時の順序と一致する

**確認手順**:
1. 複数のタブ（例: Google, GitHub, Stack Overflow）を特定の順序で開く
2. 仕事状態を保存
3. 「復元」ボタンをクリック
4. 新しいウィンドウのタブの順序を確認

**確認ポイント**:
- [ ] タブの順序が保存時と一致する

### 13.5 復元メタデータの確認

**期待される動作**:
- 復元すると、元の仕事状態に復元日時が記録される

**確認手順**:
1. 仕事状態を復元
2. Googleカレンダーで対象のイベントを確認
3. イベントの説明（description）を確認

**確認ポイント**:
- [ ] イベントの説明に`restoredTo`フィールドが追加されている
- [ ] 復元日時がISO 8601形式で記録されている

**コンソールでの確認方法**（Service Workerのコンソール）:
```javascript
// Service Workerのコンソールで実行
chrome.runtime.sendMessage({ 
  type: 'GET_WORK_STATE_EVENTS', 
  payload: { 
    startDate: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString(), 
    endDate: new Date().toISOString() 
  } 
}, (response) => {
  console.log('Work States:', response.workStates);
});
```

### 13.6 大量タブの復元確認

**期待される動作**:
- 20個以上のタブを復元する場合、段階的に読み込まれる

**確認手順**:
1. 20個以上のタブを開いた状態で仕事状態を保存
2. 「復元」ボタンをクリック
3. タブが段階的に開かれることを確認（5個ずつ、100ms間隔）

**確認ポイント**:
- [ ] 20個以上のタブが正常に復元される
- [ ] ブラウザがフリーズしない

### 13.7 エラー処理の確認

**期待される動作**:
- 認証されていない場合、エラーメッセージが表示される

**確認手順**:
1. ログアウトする
2. 保存済み仕事一覧を表示（認証が必要なため、一覧は表示されないはず）
3. 認証されていない状態では復元ボタンは表示されない

**確認ポイント**:
- [ ] 認証されていない場合、復元ボタンは表示されない

### 13.8 パフォーマンス確認

**期待される動作**:
- 10個のタブを5秒以内で復元する（NFR-001）

**確認手順**:
1. 10個のタブを開いた状態で仕事状態を保存
2. 「復元」ボタンをクリック
3. 復元にかかる時間を計測

**コンソールでの計測方法**:
```javascript
// サイドパネルのコンソールで実行
const startTime = Date.now();
// 復元ボタンをクリック
// 完了後に以下を実行
console.log(`Restore took ${Date.now() - startTime}ms`);
```

**確認ポイント**:
- [ ] 10個のタブが5秒以内で復元される

### 13.9 復元機能の確認チェックリスト

- [x] 復元ボタンが表示される
- [x] 復元中メッセージが表示される
- [x] 新しいウィンドウが開く
- [x] すべてのタブが復元される
- [x] タブの順序が保持される
- [x] 成功メッセージが表示される
- [x] 復元メタデータが記録される
- [x] 大量タブ（20個以上）が正常に復元される
- [x] 10個のタブが5秒以内で復元される

**確認完了日**: 2026-01-22

---

## 次のステップ

動作確認が完了したら、以下のBoltに進みます：

- **Bolt 7**: 復元メタデータと前後関係の可視化
- **Bolt 8-10**: その他の機能とパフォーマンス最適化

---

**作成日**: 2026-01-22  
**最終更新**: 2026-01-22  
**対象バージョン**: 0.1.0 (Bolt 1, Bolt 2, Bolt 3, Bolt 4, Bolt 5, Bolt 6完了時点)  
**Bolt 4確認完了日**: 2026-01-22  
**Bolt 5確認完了日**: 2026-01-22  
**Bolt 6確認完了日**: 2026-01-22
